# Default environment for the refactored Fair-NCL research pipeline.
# Recommended usage on AutoDL:
#   1. Choose a PyTorch + CUDA base image first.
#   2. Then run: pip install -r requirements.txt
#
# Notes:
# - torch is intentionally not pinned here because the correct build depends on
#   the CUDA version provided by the AutoDL image.
# - If your image does not already include torch, install torch first with the
#   PyTorch command that matches the image CUDA runtime, then install this file.

numpy>=1.24,<2.0
pandas>=2.0,<3.0
tqdm>=4.66,<5.0
scipy>=1.10,<2.0
scikit-learn>=1.3,<2.0
matplotlib>=3.7,<4.0
openpyxl>=3.1,<4.0
